#include <stdio.h>
/**
 * main - entry point
 * Return: 0
 */
int main(void)
{
	for (int num = 48; num <= 57; num++)
	{
		putchar(num);
	}

	for (int hi = 97; hi <= 102; hi++)
	{
		putchar(hi);
	}
	putchar('\n');
	return (0);
}
